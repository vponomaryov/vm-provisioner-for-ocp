DEVS="{{ docker_dev | default(disk_path) }}"
VG="{{ docker_vg }}"
DATA_SIZE="{{ docker_data_size }}"
STORAGE_DRIVER="{{ docker_storage_driver }}"
CONTAINER_ROOT_LV_NAME="{{ docker_container_root_lv_name }}"
CONTAINER_ROOT_LV_MOUNT_PATH="{{ docker_container_root_lv_mount_path }}"
CONTAINER_ROOT_LV_SIZE=100%FREE
